# Deploy Tools

A request-scoped doc set for the deploy toolchain.

## Scope

- `swift-docc`
- GitHub CLI (`gh`)
- `git` (tags, submodules, release hygiene)

## Next features

- Standard tool spec template per tool
- Copy/paste recipes library
- CI reference workflows (Docs build+deploy, release)
- Link checking + regressions for DocC
