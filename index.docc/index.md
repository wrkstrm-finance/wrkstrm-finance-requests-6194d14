# Requests

@Metadata {
  @PageKind(article)
  @TechnologyRoot
  @TitleHeading("Requests")
}

A semi-private, shareable hub for request-specific documentation.

This is a **provisioned** DocC bundle. It’s deployed via `swift-docc-deploy deploy provisioned-share` as a slug-hash GitHub Pages repo.

## Topics

### Deploy Tools

- <doc:deploy-tools>
